export class ColorService {
    getColors() {
        return fetch('/mock/colors.json', { headers: { 'Cache-Control': 'no-cache' } })
            .then((res) => res.json())
            .then((d) => d.data);
    }
}
