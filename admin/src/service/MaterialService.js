export class MaterialService {
    getMaterials() {
        return fetch('/mock/materials.json', { headers: { 'Cache-Control': 'no-cache' } })
            .then((res) => res.json())
            .then((d) => d.data);
    }
}
